name: validate

data:
  raw_root: data/raw
  source_timezone: UTC
  market_timezone: America/New_York
  max_tickers: null
  min_sequence_length: 12
  min_required_rows: 20000
  min_required_train_rows: 12000
  min_required_tickers: 100
  min_required_history_days: 45

universe:
  exchange: null
  asset_type: null
  tickers: null
  max_tickers: null
  min_sequence_length: 12

targets:
  horizon: 1
  threshold: 0.001
  volatility_window: 20
  zscore_window: 20
  volatility_label_k: 0.25
  regression_clip: 3.0
  primary_target: label
  direction_target: label
  threshold_target: threshold_label
  return_target: next_log_return

features:
  momentum_lookback: 3
  volatility_window: 5
  relative_volume_window: 5
  volume_window: 10
  normalize: true
  use_cross_sectional: false

dataset:
  window_size: 32
  stride: 1
  label_horizon: 1
  split_mode: global_time
  dataset_type: window
  panel_context_size: 8
  train_ratio: 0.70
  val_ratio: 0.15
  test_ratio: 0.15
  batch_size: 256
  num_workers: 0

model:
  model_name: baseline_mlp
  hidden_dims: [128, 64]
  dropout: 0.1
  minn_enabled: false
  multitask_output: false
  probabilistic_output: false
  include_rank_head: false
  include_regime_head: false
  distribution: gaussian

training:
  epochs: 4
  learning_rate: 0.001
  weight_decay: 0.0001
  scheduler_name: cosine
  scheduler_step_size: 2
  scheduler_gamma: 0.5
  device: auto
  seed: 42
  checkpoint_dir: models/checkpoints/validate
  log_path: models/logs/validate/training_log.json
  metrics_path: models/logs/validate/metrics.json
  direction_loss_weight: 1.0
  threshold_loss_weight: 0.0
  regression_loss_weight: 0.0
  rank_loss_weight: 0.0
  return_rank_weight: 0.0
  regime_loss_weight: 0.0
  regression_loss: huber
  student_t_df: 3.0
  regression_huber_delta: 1.0
  score_alignment_weight: 0.0
  score_alignment_floor: 0.10
  volatility_consistency_weight: 0.0
  temporal_smoothness_weight: 0.0
  cross_sectional_reg_weight: 0.0
  calibration_aux_weight: 0.0

backtest:
  long_threshold: 0.53
  short_threshold: 0.47
  confidence_threshold: null
  top_percentile: 0.20
  selection_mode: global_abs
  score_source: expected_utility
  long_short_percentile: null
  confidence_threshold_sweep: [0.45, 0.50, 0.55]
  confidence_top_percent_sweep: [0.05, 0.10, 0.20]
  periods_per_year: 19656
  execution_lag_bars: 1
  split_mode: global_time
  flip_positions: false
  include_costs: true
  cost_bps_per_trade: 1.0
  slippage_bps: 0.5
  signal_source: classification_prob
  require_directional_agreement: false
  confidence_mu_agreement_weight: 0.50
  signal_mu_sigma_floor: 0.10
